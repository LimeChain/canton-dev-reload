# Server-side deltas layered on top of Canton's built-in sandbox config.
# Deliberately does NOT set canton.features.enable-repair-commands: the POC records
# whether the *participant service* needs a server-side setting, or whether the repair
# gate is purely console-side (FeatureFlagFilter reads consoleEnvironment).
# Built-in defaults: node `sandbox`, ledger-api 6865, http-ledger-api 6864,
# admin-api 6866, storage = memory.
canton {}
