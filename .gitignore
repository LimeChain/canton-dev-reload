# build output and generated state
artifacts/
log/
logs/
.daml/
*.dar

# throwaway projects materialised by the variant builders; never tracked sources
.gen-*/

# loop-review sandbox (single-cycle, wiped on finalize)
.skills-workspace/

# evidence logs ARE tracked: they are the proposal's citations
!evidence/
